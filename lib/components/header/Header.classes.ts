import { ClassesBuilder } from "@utilities";

const classesConfig = {
  header: {
    default:
      "sticky top-0 left-0 right-0 w-full bg-primary p-5 px-8 flex gap-6 justify-between items-center",
  },
};

export default new ClassesBuilder(classesConfig);
